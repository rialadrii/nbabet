version: '3.8'
services:
  nba-analyzer:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./csv:/app/csv
      - ./nba.sqlite:/app/nba.sqlite
      - ./odds_cache.json:/app/odds_cache.json
    environment:
      - ODDS_API_KEY=${ODDS_API_KEY}